config:
  target: 'http://localhost:3001'
  phases:
    - duration: 1
      arrivalRate: 1000
  defaults:
    headers:
      x-my-service-auth: 
scenarios:
  - flow:
    - get:
        url: '/r/254/suggestions'
      
